在项目中有一个疑问一直缠绕着我，就是注册监听，
在整个UEP的框架中，提供了一个NotificationListener
对于告警，要实现监听上报，只要实现这接口，并且实现接口的方法，注册和销毁监听就可以。
当然不同的场景，实现不同的对象的监听，比方说ems上告警，就监听告警邮箱，北向上告警上报
就监听，北向邮箱。配置的变更，就监听配置的topic。
其实内部实现了多对多的观察者模式。不同的用户，订阅不同的主题（topic）。
