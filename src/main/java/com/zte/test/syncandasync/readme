以北向自动化测试框架中，新Socket部分，有一处是消息收割的地方。
其中实现的是异步消息转同步，因为网络IO时延的问题。
使用了接口：FutureTask，并且还有一个模板类，这个可以总结下来。
然后，借这个点，屡清楚：
1、概念：同步和异步，阻塞和非阻塞。
2、实现方式：哪些场景是同步的，哪些场景是异步的。
		 比方说：实现异步，可以使用监听，可以使用回调。
		 同步则一定是阻塞等待，知道结果返回。